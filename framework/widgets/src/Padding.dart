
Padding(
        padding: EdgeInsets.fromLTRB(30.0, 40.0, 0.0, 0.0), // from Left Top Right Bottom
        child: Column(
          // 붙일 수 있다.
        )
);

Padding(
        padding: EdgeInsets.all(10.0), // From all(상하좌우다)
);